{
  "name": "jsonserverandmock",
  "version": "1.0.0",
  "description": "json-server and mockjs ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd static && nodemon server.js",
    "dserver": "cd dynamic && nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.12.1",
    "mockjs": "^1.0.1-beta3",
    "nodemon": "^1.17.3"
  }
}
